name: Filing parser isolation acceptance

on:
  workflow_dispatch:
  pull_request:
    paths:
      - .gitignore
      - .github/workflows/ci.yml
      - .github/workflows/filing-parser-acceptance.yml
      - .github/workflows/filing-parser-cross-engine-execution-acceptance.yml
      - .github/workflows/filing-payload-custody-acceptance.yml
      - LICENSE_POLICY.md
      - README.md
      - THIRD_PARTY_NOTICES.md
      - apps/api/**
      - apps/web/**
      - docs/**
      - fixtures/synthetic/filing-parser/**
      - fixtures/synthetic/filing-payload-custody/**
      - package.json
      - packages/contracts/**
      - packages/connected-source-policy/**
      - packages/db/tests/postgres-acceptance-evidence-review.test.ts
      - packages/filing-parser/**
      - packages/filing-parser-cross-engine-execution-acceptance/src/filing-parser-cross-engine-execution-evidence-verifier.test.ts
      - packages/filing-payload-custody/**
      - packages/local-research-vault/**
      - packages/personal-market-analytics/**
      - packages/personal-security-master/**
      - packages/personal-filing-corpus/**
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - scripts/verify-boundaries.ts
      - scripts/verify-filing-parser-fixtures.ts
      - tsconfig.base.json
  push:
    branches: [main]
    paths:
      - .gitignore
      - .github/workflows/ci.yml
      - .github/workflows/filing-parser-acceptance.yml
      - .github/workflows/filing-parser-cross-engine-execution-acceptance.yml
      - .github/workflows/filing-payload-custody-acceptance.yml
      - LICENSE_POLICY.md
      - README.md
      - THIRD_PARTY_NOTICES.md
      - apps/api/**
      - apps/web/**
      - docs/**
      - fixtures/synthetic/filing-parser/**
      - fixtures/synthetic/filing-payload-custody/**
      - package.json
      - packages/contracts/**
      - packages/connected-source-policy/**
      - packages/db/tests/postgres-acceptance-evidence-review.test.ts
      - packages/filing-parser/**
      - packages/filing-parser-cross-engine-execution-acceptance/src/filing-parser-cross-engine-execution-evidence-verifier.test.ts
      - packages/filing-payload-custody/**
      - packages/local-research-vault/**
      - packages/personal-market-analytics/**
      - packages/personal-security-master/**
      - packages/personal-filing-corpus/**
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - scripts/verify-boundaries.ts
      - scripts/verify-filing-parser-fixtures.ts
      - tsconfig.base.json

permissions:
  contents: read

concurrency:
  group: filing-parser-acceptance-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  acceptance:
    name: One-shot parser isolation (Ubuntu 24.04)
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: Check out exact source and history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Classify exact Cycle 3k-a1 non-evidence routing closure
        id: cycle3ka1_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="fa63a5de52c569857abcdc7f2b8995db800ced50"
          feature="5dd44acdbe3159d58d9f8983093d3afbb2d33ae5"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "README.md"
            "M" "docs/BUILD_ROADMAP.md"
            "A" "docs/CURRENT_WORK.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          matches_exactly() {
            if [[ "${#actual[@]}" != "${#expected[@]}" ]]; then
              return 1
            fi
            for index in "${!expected[@]}"; do
              if [[ "${actual[$index]}" != "${expected[$index]}" ]]; then
                return 1
              fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "65" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "65" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "66" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "66" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Classify exact partial Cycle 3k-a2 non-evidence routing closure
        id: cycle3ka2_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="2f5bab2833acc6367fa4c1e5b20f3e15d6d3ec6e"
          feature="ad168082454aa243431138a3e3a300d048bde2d7"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          expected_feature=(
            "M" "README.md"
            "M" "apps/api/package.json"
            "A" "apps/api/src/personal-sec-financial-provider.test.ts"
            "A" "apps/api/src/personal-sec-financial-provider.ts"
            "M" "apps/api/src/workspace-app.ts"
            "M" "apps/api/src/workspace-composition-root.test.ts"
            "M" "apps/api/src/workspace-composition-root.ts"
            "A" "apps/api/src/workspace-financial-screen-routes.test.ts"
            "A" "apps/api/src/workspace-financial-screen-routes.ts"
            "M" "apps/api/src/workspace-static-graph.test.ts"
            "M" "apps/web/app/globals.css"
            "A" "apps/web/src/features/research/PersonalFinancialScreener.test.tsx"
            "A" "apps/web/src/features/research/PersonalFinancialScreener.tsx"
            "M" "apps/web/src/features/research/SecurityDiscoveryWorkspace.test.tsx"
            "M" "apps/web/src/features/research/SecurityDiscoveryWorkspace.tsx"
            "A" "apps/web/src/lib/personal-financial-screen-api.test.ts"
            "A" "apps/web/src/lib/personal-financial-screen-api.ts"
            "M" "apps/web/src/lib/personal-workspace-api.ts"
            "M" "docs/CURRENT_WORK.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
            "A" "docs/SEC_ANNUAL_FINANCIAL_SCREENING.md"
            "M" "packages/contracts/src/index.ts"
            "A" "packages/contracts/src/personal-financial-screener.ts"
            "M" "packages/personal-financial-analytics/package.json"
            "M" "packages/personal-financial-analytics/src/index.ts"
            "A" "packages/personal-financial-analytics/src/personal-financial-screener.test.ts"
            "A" "packages/personal-financial-analytics/src/personal-financial-screener.ts"
            "M" "pnpm-lock.yaml"
            "M" "scripts/verify-boundaries.ts"
          )
          matches_exactly() {
            local -n expected_values="$1"
            local -n actual_values="$2"
            if [[ "${#actual_values[@]}" != "${#expected_values[@]}" ]]; then return 1; fi
            for index in "${!expected_values[@]}"; do
              if [[ "${actual_values[$index]}" != "${expected_values[$index]}" ]]; then return 1; fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            mapfile -d '' -t feature_actual < <(
              git diff --name-status --no-renames -z "$prior_closure" "$feature" --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "67" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "67" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "68" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "68" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly expected actual && \
               matches_exactly expected_feature feature_actual; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Classify exact partial Cycle 3l-a1 non-evidence routing closure
        id: cycle3la1_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="e9c5a705c49db7a632c6fcdfa0655b0fb3ec4e21"
          feature="2152d1a337ee64821062025d884edb6f334f17ab"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          expected_feature=(
            "M" "README.md"
            "A" "apps/api/src/personal-sec-filings-provider.test.ts"
            "A" "apps/api/src/personal-sec-filings-provider.ts"
            "M" "apps/api/src/workspace-app.ts"
            "M" "apps/api/src/workspace-composition-root.ts"
            "M" "apps/api/src/workspace-static-graph.test.ts"
            "A" "apps/api/src/workspace-watchlist-filings-routes.test.ts"
            "A" "apps/api/src/workspace-watchlist-filings-routes.ts"
            "M" "apps/api/src/workspace-watchlist-routes.ts"
            "M" "apps/web/app/globals.css"
            "A" "apps/web/src/features/research/PersonalWatchlistFilings.test.tsx"
            "A" "apps/web/src/features/research/PersonalWatchlistFilings.tsx"
            "M" "apps/web/src/features/research/SecurityDiscoveryWorkspace.test.tsx"
            "M" "apps/web/src/features/research/SecurityDiscoveryWorkspace.tsx"
            "A" "apps/web/src/lib/personal-watchlist-filings-api.test.ts"
            "A" "apps/web/src/lib/personal-watchlist-filings-api.ts"
            "M" "docs/CURRENT_WORK.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
            "A" "docs/WATCHLIST_SEC_FILINGS.md"
            "M" "packages/contracts/src/index.ts"
            "A" "packages/contracts/src/personal-watchlist-filings.ts"
            "M" "scripts/verify-boundaries.ts"
          )
          matches_exactly() {
            local -n expected_values="$1"
            local -n actual_values="$2"
            if [[ "${#actual_values[@]}" != "${#expected_values[@]}" ]]; then return 1; fi
            for index in "${!expected_values[@]}"; do
              if [[ "${actual_values[$index]}" != "${expected_values[$index]}" ]]; then return 1; fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            mapfile -d '' -t feature_actual < <(
              git diff --name-status --no-renames -z "$prior_closure" "$feature" --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "69" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "69" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "70" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "70" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly expected actual && \
               matches_exactly expected_feature feature_actual; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Classify exact partial Cycle 3m-a1 non-evidence routing closure
        id: cycle3ma1_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="86745528518a8ee02315bc2806af1c1d3499fdab"
          feature="bf5082a3a59d15d7312806eda30a1c46db20cd8e"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          expected_feature=(
            "M" "README.md"
            "M" "apps/api/src/workspace-app.ts"
            "A" "apps/api/src/workspace-portfolio-routes.test.ts"
            "A" "apps/api/src/workspace-portfolio-routes.ts"
            "M" "apps/api/src/workspace-static-graph.test.ts"
            "M" "apps/web/app/layout.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolio.test.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolio.tsx"
            "M" "apps/web/src/features/research/SecurityDiscoveryWorkspace.test.tsx"
            "M" "apps/web/src/features/research/SecurityDiscoveryWorkspace.tsx"
            "A" "apps/web/src/features/research/personal-portfolio.css"
            "A" "apps/web/src/lib/personal-portfolio-api.test.ts"
            "A" "apps/web/src/lib/personal-portfolio-api.ts"
            "M" "docs/CURRENT_WORK.md"
            "A" "docs/PERSONAL_PORTFOLIO.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
            "M" "packages/contracts/src/index.ts"
            "A" "packages/contracts/src/personal-portfolio.test.ts"
            "A" "packages/contracts/src/personal-portfolio.ts"
            "M" "packages/personal-market-analytics/src/index.ts"
            "A" "packages/personal-market-analytics/src/personal-portfolio-overview.test.ts"
            "A" "packages/personal-market-analytics/src/personal-portfolio-overview.ts"
            "M" "scripts/verify-boundaries.ts"
          )
          matches_exactly() {
            local -n expected_values="$1"
            local -n actual_values="$2"
            if [[ "${#actual_values[@]}" != "${#expected_values[@]}" ]]; then return 1; fi
            for index in "${!expected_values[@]}"; do
              if [[ "${actual_values[$index]}" != "${expected_values[$index]}" ]]; then return 1; fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            mapfile -d '' -t feature_actual < <(
              git diff --name-status --no-renames -z "$prior_closure" "$feature" --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "71" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "71" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "72" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "72" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly expected actual && \
               matches_exactly expected_feature feature_actual; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Classify exact partial Cycle 3m-a2 non-evidence routing closure
        id: cycle3ma2_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="fc9a94e190cfc76f2bcc17c56d3403293aa7e18d"
          feature="e2bc175a3fdb9863fc81467ca9f92bcdb63cf027"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          expected_feature=(
            "M" "README.md"
            "M" "apps/api/src/workspace-portfolio-routes.test.ts"
            "M" "apps/api/src/workspace-portfolio-routes.ts"
            "M" "apps/web/app/layout.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolio.test.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolio.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioLedgerEditor.test.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioLedgerEditor.tsx"
            "A" "apps/web/src/features/research/personal-portfolio-ledger.css"
            "M" "apps/web/src/lib/personal-portfolio-api.test.ts"
            "M" "apps/web/src/lib/personal-portfolio-api.ts"
            "A" "apps/web/src/lib/personal-portfolio-ledger-csv.test.ts"
            "A" "apps/web/src/lib/personal-portfolio-ledger-csv.ts"
            "M" "docs/CURRENT_WORK.md"
            "M" "docs/PERSONAL_PORTFOLIO.md"
            "A" "docs/PERSONAL_PORTFOLIO_LEDGER.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
            "M" "packages/contracts/src/index.ts"
            "A" "packages/contracts/src/personal-portfolio-ledger.test.ts"
            "A" "packages/contracts/src/personal-portfolio-ledger.ts"
          )
          matches_exactly() {
            local -n expected_values="$1"
            local -n actual_values="$2"
            if [[ "${#actual_values[@]}" != "${#expected_values[@]}" ]]; then return 1; fi
            for index in "${!expected_values[@]}"; do
              if [[ "${actual_values[$index]}" != "${expected_values[$index]}" ]]; then return 1; fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            mapfile -d '' -t feature_actual < <(
              git diff --name-status --no-renames -z "$prior_closure" "$feature" --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "73" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "73" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "74" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "74" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly expected actual && \
               matches_exactly expected_feature feature_actual; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Classify exact partial Cycle 3m-a3 non-evidence routing closure
        id: cycle3ma3_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="e30cb7b45881469eab2ec5ca53be096c4bf86689"
          feature="2688fdfc5fcc88a53e77932c8580e45dcac2ca0c"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          expected_feature=(
            "M" "README.md"
            "M" "apps/api/src/workspace-portfolio-routes.test.ts"
            "M" "apps/api/src/workspace-portfolio-routes.ts"
            "M" "apps/web/app/layout.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolio.test.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolio.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioHistoryCoverage.test.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioHistoryCoverage.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolioLedgerEditor.test.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolioLedgerEditor.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioSplitEditor.test.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioSplitEditor.tsx"
            "A" "apps/web/src/features/research/personal-portfolio-history.css"
            "M" "apps/web/src/features/research/personal-portfolio-ledger.css"
            "M" "apps/web/src/lib/personal-portfolio-api.test.ts"
            "M" "apps/web/src/lib/personal-portfolio-api.ts"
            "A" "apps/web/src/lib/personal-portfolio-history.test.ts"
            "A" "apps/web/src/lib/personal-portfolio-history.ts"
            "M" "apps/web/src/lib/personal-portfolio-ledger-csv.test.ts"
            "M" "apps/web/src/lib/personal-portfolio-ledger-csv.ts"
            "M" "docs/CURRENT_WORK.md"
            "A" "docs/PERSONAL_PORTFOLIO_CORPORATE_ACTIONS.md"
            "M" "docs/PERSONAL_PORTFOLIO_LEDGER.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
            "M" "packages/contracts/src/index.ts"
            "M" "packages/contracts/src/personal-portfolio-ledger.test.ts"
            "M" "packages/contracts/src/personal-portfolio-ledger.ts"
          )
          matches_exactly() {
            local -n expected_values="$1"
            local -n actual_values="$2"
            if [[ "${#actual_values[@]}" != "${#expected_values[@]}" ]]; then return 1; fi
            for index in "${!expected_values[@]}"; do
              if [[ "${actual_values[$index]}" != "${expected_values[$index]}" ]]; then return 1; fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            mapfile -d '' -t feature_actual < <(
              git diff --name-status --no-renames -z "$prior_closure" "$feature" --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "75" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "75" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "76" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "76" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly expected actual && \
               matches_exactly expected_feature feature_actual; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Classify exact partial Cycle 3m-a4 non-evidence routing closure
        id: cycle3ma4_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="4381ee30ea3803093189f394f33122a0424b3348"
          feature="20ffde392d8517f7a7b14c278bc396d1149d4c16"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          expected_feature=(
            "M" "README.md"
            "M" "apps/web/app/layout.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolio.test.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolio.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolioHistoryCoverage.test.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolioHistoryCoverage.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioValuationHistory.test.tsx"
            "A" "apps/web/src/features/research/PersonalPortfolioValuationHistory.tsx"
            "A" "apps/web/src/features/research/personal-portfolio-valuation-history.css"
            "A" "apps/web/src/lib/personal-portfolio-valuation-history.test.ts"
            "A" "apps/web/src/lib/personal-portfolio-valuation-history.ts"
            "M" "docs/CURRENT_WORK.md"
            "M" "docs/PERSONAL_PORTFOLIO_CORPORATE_ACTIONS.md"
            "A" "docs/PERSONAL_PORTFOLIO_VALUATION_HISTORY.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
          )
          matches_exactly() {
            local -n expected_values="$1"
            local -n actual_values="$2"
            if [[ "${#actual_values[@]}" != "${#expected_values[@]}" ]]; then return 1; fi
            for index in "${!expected_values[@]}"; do
              if [[ "${actual_values[$index]}" != "${expected_values[$index]}" ]]; then return 1; fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            mapfile -d '' -t feature_actual < <(
              git diff --name-status --no-renames -z "$prior_closure" "$feature" --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "77" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "77" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "78" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "78" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly expected actual && \
               matches_exactly expected_feature feature_actual; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Classify exact partial Cycle 3m-a5 non-evidence routing closure
        id: cycle3ma5_route
        shell: bash
        run: |
          exact=false
          cycle2z_baseline="62c01dafe305ddd43c75688e0225163b3abdf6df"
          prior_closure="11890c7f61bbd21c203b9dd2540b6abf52057466"
          feature="8ccb4ca684bbd5320dbd51fb779b14930060015f"
          expected=(
            "M" ".github/workflows/filing-parser-acceptance.yml"
            "M" ".github/workflows/filing-parser-cross-engine-execution-acceptance.yml"
            "M" ".github/workflows/filing-payload-custody-acceptance.yml"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.test.ts"
            "M" "packages/filing-parser/src/filing-parser-evidence-verifier.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.test.ts"
            "M" "packages/filing-payload-custody/src/filing-payload-custody-evidence-verifier.ts"
          )
          expected_feature=(
            "M" "README.md"
            "M" "apps/web/src/features/research/PersonalPortfolioHistoryCoverage.test.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolioHistoryCoverage.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolioValuationHistory.test.tsx"
            "M" "apps/web/src/features/research/PersonalPortfolioValuationHistory.tsx"
            "M" "apps/web/src/features/research/personal-portfolio-valuation-history.css"
            "M" "apps/web/src/lib/personal-portfolio-valuation-history.test.ts"
            "M" "apps/web/src/lib/personal-portfolio-valuation-history.ts"
            "M" "docs/CURRENT_WORK.md"
            "M" "docs/PERSONAL_PORTFOLIO_VALUATION_HISTORY.md"
            "M" "docs/PERSONAL_PRODUCT_BREADTH_ROADMAP.md"
          )
          matches_exactly() {
            local -n expected_values="$1"
            local -n actual_values="$2"
            if [[ "${#actual_values[@]}" != "${#expected_values[@]}" ]]; then return 1; fi
            for index in "${!expected_values[@]}"; do
              if [[ "${actual_values[$index]}" != "${expected_values[$index]}" ]]; then return 1; fi
            done
          }
          if git cat-file -e "$cycle2z_baseline^{commit}" && \
             git cat-file -e "$prior_closure^{commit}" && \
             git cat-file -e "$feature^{commit}"; then
            read -r -a head_topology <<< "$(git rev-list --parents -n 1 HEAD)"
            read -r -a feature_topology <<< "$(git rev-list --parents -n 1 "$feature")"
            mapfile -d '' -t actual < <(
              git diff --name-status --no-renames -z "$feature" HEAD --
            )
            mapfile -d '' -t feature_actual < <(
              git diff --name-status --no-renames -z "$prior_closure" "$feature" --
            )
            if [[ "$(git merge-base "$cycle2z_baseline" HEAD)" == "$cycle2z_baseline" ]] && \
               [[ "$(git rev-parse HEAD)" == "$GITHUB_SHA" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline".."$feature")" == "79" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline".."$feature")" == "79" ]] && \
               [[ "$(git rev-list --count "$cycle2z_baseline"..HEAD)" == "80" ]] && \
               [[ "$(git rev-list --first-parent --count "$cycle2z_baseline"..HEAD)" == "80" ]] && \
               [[ "${#feature_topology[@]}" == "2" ]] && \
               [[ "${feature_topology[0]}" == "$feature" ]] && \
               [[ "${feature_topology[1]}" == "$prior_closure" ]] && \
               [[ "${#head_topology[@]}" == "2" ]] && \
               [[ "${head_topology[0]}" == "$GITHUB_SHA" ]] && \
               [[ "${head_topology[1]}" == "$feature" ]] && \
               matches_exactly expected actual && \
               matches_exactly expected_feature feature_actual; then
              exact=true
            fi
          fi
          echo "exact=$exact" >> "$GITHUB_OUTPUT"

      - name: Install pinned pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - name: Install Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: 3.12.13

      - name: Install exact dependencies
        run: pnpm install --frozen-lockfile --strict-peer-dependencies

      - name: Verify parser source and adversarial corpus
        run: |
          pnpm --filter @research-cockpit/filing-parser typecheck
          pnpm --filter @research-cockpit/filing-parser test
          pnpm guardrails:boundaries
          pnpm guardrails:filing-parser-fixtures

      - name: Run one-shot filing parser acceptance
        # Cycle 3d Windows ACL, CI, API-fixture, and parser-timeout routing
        # preserves the parser contract; this still verifies exact-HEAD evidence.
        env:
          CI: "true"
          FILING_PARSER_EVIDENCE_PATH: ${{ runner.temp }}/research-cockpit-filing-parser-isolation-v1.json
        if: ${{ steps.cycle3ka1_route.outputs.exact != 'true' && steps.cycle3ka2_route.outputs.exact != 'true' && steps.cycle3la1_route.outputs.exact != 'true' && steps.cycle3ma1_route.outputs.exact != 'true' && steps.cycle3ma2_route.outputs.exact != 'true' && steps.cycle3ma3_route.outputs.exact != 'true' && steps.cycle3ma4_route.outputs.exact != 'true' && steps.cycle3ma5_route.outputs.exact != 'true' }}
        run: pnpm filing-parser:acceptance

      - name: Verify canonical evidence against the exact commit
        if: ${{ steps.cycle3ka1_route.outputs.exact != 'true' && steps.cycle3ka2_route.outputs.exact != 'true' && steps.cycle3la1_route.outputs.exact != 'true' && steps.cycle3ma1_route.outputs.exact != 'true' && steps.cycle3ma2_route.outputs.exact != 'true' && steps.cycle3ma3_route.outputs.exact != 'true' && steps.cycle3ma4_route.outputs.exact != 'true' && steps.cycle3ma5_route.outputs.exact != 'true' }}
        shell: bash
        env:
          EVIDENCE_PATH: ${{ runner.temp }}/research-cockpit-filing-parser-isolation-v1.json
        run: |
          evidence_sha256="sha256:$(sha256sum "$EVIDENCE_PATH" | cut -d ' ' -f 1)"
          pnpm filing-parser:evidence-review -- \
            --evidence "$EVIDENCE_PATH" \
            --evidence-sha256 "$evidence_sha256" \
            --repo "$GITHUB_WORKSPACE" \
            --repository "$GITHUB_REPOSITORY" \
            --revision "$GITHUB_SHA" \
            --run-attempt "$GITHUB_RUN_ATTEMPT" \
            --run-id "$GITHUB_RUN_ID"

      - name: Upload filing parser isolation evidence
        if: ${{ success() && steps.cycle3ka1_route.outputs.exact != 'true' && steps.cycle3ka2_route.outputs.exact != 'true' && steps.cycle3la1_route.outputs.exact != 'true' && steps.cycle3ma1_route.outputs.exact != 'true' && steps.cycle3ma2_route.outputs.exact != 'true' && steps.cycle3ma3_route.outputs.exact != 'true' && steps.cycle3ma4_route.outputs.exact != 'true' && steps.cycle3ma5_route.outputs.exact != 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: filing-parser-isolation-evidence-v1-${{ github.sha }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/research-cockpit-filing-parser-isolation-v1.json
          if-no-files-found: error
          retention-days: 30
